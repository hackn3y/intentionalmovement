# Mobile App Environment Variables

# Backend API URL
# Development: Use localhost for iOS simulator, 10.0.2.2 for Android emulator
# Physical device: Replace with your computer's IP address
EXPO_PUBLIC_API_URL=http://localhost:3001/api

# To find your computer's IP address:
# Windows: ipconfig (look for IPv4 Address)
# Mac/Linux: ifconfig (look for inet)
# Example for physical device: EXPO_PUBLIC_API_URL=http://192.168.1.100:3001/api

# Stripe Configuration (Required for purchases)
# Get test publishable key from: https://dashboard.stripe.com/test/apikeys
# IMPORTANT: Use test keys (pk_test_...) for development, not live keys
EXPO_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# Firebase Configuration (Optional - for push notifications and analytics)
# Create Firebase project at: https://console.firebase.google.com
# Download google-services.json (Android) and GoogleService-Info.plist (iOS)
FIREBASE_API_KEY=your_firebase_api_key
FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
FIREBASE_PROJECT_ID=your_project_id
FIREBASE_STORAGE_BUCKET=your_project.appspot.com
FIREBASE_MESSAGING_SENDER_ID=your_sender_id
FIREBASE_APP_ID=your_app_id

# App Metadata
APP_NAME=Intentional Movement
APP_VERSION=1.0.0
ENVIRONMENT=development

# Notes:
# - All variables prefixed with EXPO_PUBLIC_ are exposed to the client
# - Never put secret keys in these variables
# - For production, use Expo's EAS Secrets for sensitive values
