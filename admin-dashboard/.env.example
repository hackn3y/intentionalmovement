# Admin Dashboard Environment Variables

# Backend API URL
# Development: localhost
# Production: Your deployed backend URL
REACT_APP_API_URL=http://localhost:3001/api

# App Configuration
REACT_APP_NAME=Intentional Movement Admin
REACT_APP_VERSION=1.0.0

# Optional: Analytics
# REACT_APP_GA_TRACKING_ID=your_google_analytics_id

# Optional: Error Tracking
# REACT_APP_SENTRY_DSN=your_sentry_dsn

# Notes:
# - All variables must be prefixed with REACT_APP_
# - Variables are embedded into the build at compile time
# - Changes require rebuilding the app (npm run build)
# - Never put secret keys here - they will be visible in the client bundle
